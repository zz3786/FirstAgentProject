# 向量库选择与融合式架构 · 常见问题总结

> 本文总结 RAG 学习中关于"向量库选型、MySQL 为什么不推荐、融合式与分离式架构取舍"的核心问题

---

## 一、向量库该如何选择

### 核心原则

> **先跑通，再选型，不要一上来就追求"大而全"的复杂方案。**

### 第一步：明确核心需求

在选择之前，先问自己几个问题：

| 维度 | 关键问题 |
|------|---------|
| **数据规模** | 百万级？亿级？ |
| **查询模式** | 纯语义搜索？还是需要业务字段过滤？ |
| **运维能力** | 能否额外维护一套独立向量库？还是想复用现有数据库？ |
| **性能要求** | 毫秒级？还是几十毫秒可接受？ |

### 第二步：理解两条技术路线

| 路线 | 代表 | 核心优势 | 代价 |
|------|------|---------|------|
| **融合型**（传统库扩展） | PostgreSQL + pgvector | 架构简单，向量与业务同库，一条 SQL 搞定 | 极致性能不如独立向量库 |
| **独立型**（专用向量库） | Milvus、Qdrant、Weaviate | 极致性能，亿级向量毫秒延迟 | 额外部署和维护成本 |

### 第三步：主流向量库对比

| 向量库 | 类型 | 核心优势 | 关键考量 | Spring AI 集成 |
|:---|:---|:---|:---|:---|
| **pgvector** | 融合型 | 与 PostgreSQL 无缝集成，运维简单 | 亿级以上性能不如独立向量库 | 原生支持，官方 Starter |
| **Qdrant** | 独立型 | 性能与简洁性最佳平衡，延迟极低 | 需独立部署 | 官方 Starter |
| **Milvus** | 独立型 | 功能全面，CNCF 项目，支持十亿级 | 架构复杂，运维成本高 | 官方 Starter |
| **Redis Stack** | 融合型 | 极致速度，可复用已有 Redis | 需启用 Redis Stack 模块 | 官方 Starter |
| **Weaviate** | 独立型 | 混合搜索强（稠密+稀疏） | 资源消耗较高 | 官方 Starter |

### 第四步：推荐路径

**学习阶段**：从 **pgvector** 开始

- 学习成本最低，本质是 PostgreSQL
- Spring AI 开箱即用，配置极少
- 百万级数据性能足够

**生产阶段**：按场景选择

| 场景 | 推荐 |
|------|------|
| 已有 Redis | **Redis Stack**（复用基础设施） |
| 数据量大（> 1000 万） | Qdrant / Milvus |
| 已有 PostgreSQL | pgvector |
| 只有 MySQL | Redis Stack 或 Qdrant |
| 极致一体化 | 迁业务到 PostgreSQL |

---

## 二、为什么不推荐 MySQL

### 结论

**MySQL 的向量能力仍处于"追赶阶段"，生态适配不成熟。**

### 原因 1：基因不同——"补丁"式 vs "原生"扩展

| 数据库 | 向量能力来源 |
|--------|------------|
| **MySQL** | 后加的功能。社区版 MySQL 9.0 才引入 `VECTOR` 类型，但**关键的 ANN 近似索引在社区版仍缺失** |
| **PostgreSQL + pgvector** | 从一开始就为 AI 场景设计的原生扩展，支持 HNSW、IVFFlat 等主流索引 |

阿里云 RDS 等企业版虽提供了 HNSW 索引，但属于**商业闭源方案**。

### 原因 2：性能与功能差距明显

| 维度 | MySQL 社区版 | pgvector |
|------|:---:|:---:|
| 检索方式 | 全表扫描（暴力搜索） | ANN 索引（近似搜索） |
| 延迟（50 万条） | 3 秒+ | 毫秒级 |
| 性能差距 | 100 倍以上 | - |
| 距离函数 | 基础 | 完整（余弦/内积/欧氏） |
| 索引类型 | 缺失 | HNSW / IVFFlat |

### 原因 3：Spring AI 生态缺位

| 向量库 | Spring AI Starter |
|--------|:---:|
| pgvector | ✅ 官方 |
| Redis Stack | ✅ 官方 |
| Qdrant | ✅ 官方 |
| Milvus | ✅ 官方 |
| **MySQL** | ❌ **无官方 Starter** |

**用 MySQL 意味着**：需要手动实现 `VectorStore` 接口，处理 SQL 方言、序列化、索引创建等底层细节。

**你已经体会过这种复杂度**——项目里的 `RedisChatMemoryRepository` 就是手写实现的。

### 何时可以考虑 MySQL

- 数据量 < 10 万条
- 已有重度 MySQL 技术栈，希望零额外运维
- 数据一致性要求极高（业务和向量同事务）
- 内部后台系统，对延迟不苛刻

---

## 三、向量库和业务库融合有什么好处

**融合式**（典型代表：PostgreSQL + pgvector）的核心价值：**用一套系统同时处理"业务数据"和"向量数据"**。

### 好处 1：一条 SQL 搞定"过滤 + 检索"（最大价值）

**场景**：用户问"我订单里的商品说明书里有没有提到保修？"

**分离式**：

```text
① 去 MySQL 查该用户订单关联哪些商品
② 得到商品 ID 列表
③ 去 Milvus 查这些商品的说明书 embedding
④ 应用层合并结果
   ↑ 两次查询、两个系统、应用层做 join
```

**融合式**：

```sql
SELECT d.content, d.metadata
FROM documents d
JOIN user_orders uo ON d.product_id = uo.product_id
WHERE uo.user_id = 'user-alice'
  AND uo.status = 'PAID'
ORDER BY d.embedding <=> '[0.21, -0.45, ...]'::vector
LIMIT 5;
```

**一条 SQL 完成**——向量检索 + 业务过滤 + 关联查询。

### 好处 2：事务一致性

**场景**：上传文档时，同时存文档元数据 + embedding。

**分离式**：

```text
① MySQL 插入文档记录 ✓
② Milvus 插入 embedding ✗（网络失败）
   ↓
数据不一致：MySQL 有文档，但向量库里没有
需要补偿机制、重试逻辑、对账任务
```

**融合式**：

```sql
BEGIN;
INSERT INTO documents (...) VALUES (...);
INSERT INTO document_embeddings (...) VALUES (...);
COMMIT;
   ↑ 要么都成功，要么都失败
```

### 好处 3：运维成本降低

| 维度 | 分离式 | 融合式 |
|------|:---:|:---:|
| 部署组件 | 2 个 | 1 个 |
| 监控系统 | 2 套 | 1 套 |
| 备份策略 | 2 套 | 1 套 |
| 故障排查 | 跨系统 | 单系统 |

**收益**：**少维护一个系统 = 少一个半夜告警**。

### 好处 4：向量和业务数据天然关联

```sql
SELECT 
    e.content AS chunk,
    d.title, d.author, d.upload_time   -- 元信息直接 join
FROM document_embeddings e
JOIN documents d ON e.doc_id = d.id
ORDER BY e.embedding <=> '[...]'::vector
LIMIT 5;
   ↓ 一次查询拿到所有信息
```

### 好处 5：权限过滤天然可用

```sql
SELECT e.content
FROM document_embeddings e
JOIN documents d ON e.doc_id = d.id
JOIN user_permissions p ON d.dept_id = p.dept_id
WHERE p.user_id = 'user-alice'
ORDER BY e.embedding <=> '[...]'::vector
LIMIT 5;
```

### 好处 6：复用现有基础设施

不需要引入新数据库、不需要新学运维工具、直接复用连接池、监控、备份体系。

### 融合式的代价

| 代价 | 说明 |
|------|------|
| 性能上限 | 亿级向量时不如专业向量库 |
| 资源竞争 | 向量查询可能影响业务查询 |
| 扩展性 | 水平扩展不如分布式向量库 |
| 功能深度 | 不如 Milvus 的专业功能 |

### 关键判断：数据量级

| 数据规模 | 推荐 |
|:---:|------|
| < 100 万 | **融合式（pgvector）绝对够用** |
| 100 万 ~ 1000 万 | 融合式仍可，注意索引优化 |
| > 1000 万 | 考虑独立向量库 |
| > 1 亿 | 必须独立向量库 |

---

## 四、公司已有 MySQL，该怎么办

### 核心结论

> **融合不是必须，只是选项之一。**
>
> **不要为了 pgvector 硬上 PostgreSQL**——除非你打算把业务数据也迁过去。

### 先破除一个误区

**"向量库"和"业务库"不一定要融合。**

三种典型方案：

#### 方案 A：MySQL + 独立向量库（Qdrant / Milvus）

```text
业务：MySQL（不动）
向量：Qdrant（新引入）
```

- 运维成本：新增一个 Qdrant
- 团队技能：MySQL 不动，Qdrant 学习成本低
- Spring AI：官方 Starter

**适合**：团队不想动 MySQL，愿意接受"多一个组件"。

#### 方案 B：MySQL + Redis Stack（最省心）

```text
业务：MySQL（不动）
向量：Redis Stack（启用 RediSearch 模块）
```

- 运维成本：**换 Redis 镜像即可**
- 团队技能：复用现有 Redis 知识
- 性能：单机性能极强
- Spring AI：官方 Starter

**适合**：**已有 Redis 且不想引入全新数据库**。

#### 方案 C：PostgreSQL + pgvector（不推荐）

```text
业务：MySQL（保留）
向量：PostgreSQL + pgvector（新引入）
   ↑ 两套数据库并存——更复杂
```

**注意**：如果业务数据**不迁到 PostgreSQL**，pgvector 的"融合优势"**根本用不上**——和用 Qdrant 没本质区别，但 PostgreSQL 运维成本更高。

### 为什么融合优势用不上

如果向量库在 PostgreSQL，业务数据在 MySQL：

```text
① 从 PostgreSQL 检索 Top-K
② 拿 ID 回 MySQL 查用户 ID 过滤
③ 过滤掉不属于该用户的
   ↑ 还是要跨两个库
```

**和分离式完全一样**，只是"分离"的两个库换成了 PostgreSQL 和 MySQL。

### 决策框架

| 场景 | 推荐方案 |
|------|---------|
| **学习项目** | pgvector（学思想，上手快） |
| **生产：已有 Redis** | **Redis Stack**（复用基础设施） |
| **生产：数据量大** | Qdrant / Milvus |
| **生产：已有 PostgreSQL** | pgvector |
| **生产：只有 MySQL** | **Redis Stack 或 Qdrant** |
| **生产：极致一体化** | 迁业务到 PostgreSQL（代价最大） |

### 关键判断

**向量库和业务库一定要同库吗？**

不一定。看两个问题：

| 问题 | 融合式 | 分离式 |
|------|:---:|:---:|
| **检索时需要业务过滤吗** | 一条 SQL 搞定 | 应用层合并，或向量库 metadata filter |
| **数据一致性要求高吗** | 事务天然保证 | 需补偿机制（RAG 场景可接受） |

**现代向量库（Qdrant / Milvus / Redis Stack）都支持 metadata filter**——分离式也能做到，只是性能略差。

**结论**：大多数 RAG 场景，**分离式完全可以胜任**。

---

## 五、针对本项目的具体建议

### 学习阶段（当前）

```text
agent-rag 模块：
  - 使用 pgvector
  - 目的是学 RAG 完整流程
  - 未来可平滑切换
```

**理由**：

1. 项目还没到生产，学习 pgvector 能理解"融合式"思想
2. 学习目标是理解 RAG 全流程，pgvector 上手最快
3. 未来迁移到其他向量库，`VectorStore` 抽象层让切换成本很低

### 生产阶段（未来）

**推荐方案 B：MySQL + Redis Stack**

| 理由 | 说明 |
|------|------|
| **复用现有 Redis** | 项目已在用，运维成本接近 0 |
| **不引入新数据库** | 只需换 Redis 镜像（Stack 版） |
| **团队技能复用** | Redis 团队已熟悉 |
| **Spring AI 支持** | 有官方 Starter |
| **性能足够** | 中小规模 QPS 比 pgvector 还高 |

**如果 Redis Stack 不够用**（数据量 > 1000 万），再上 **Qdrant** 或 **Milvus**。

---

## 六、核心要点总结

| # | 要点 |
|:---:|------|
| 1 | 向量库选择：**先跑通，再选型**，不必追求大而全 |
| 2 | MySQL 向量能力**不成熟**：无 ANN 索引、无 Spring AI Starter、性能差 100 倍 |
| 3 | 融合式的核心价值：**一条 SQL 同时做"向量检索 + 业务过滤 + 关联查询"** |
| 4 | 融合式的代价：性能上限低，**只适合 < 1000 万数据量** |
| 5 | **融合不是必须**——分离式在大多数 RAG 场景完全够用 |
| 6 | 公司已有 MySQL：**不要为了 pgvector 硬上 PostgreSQL** |
| 7 | **生产优先 Redis Stack**——复用现有 Redis，成本最低 |
| 8 | 学习阶段用 pgvector 学思想，生产按场景切换 |

---

## 七、类比记忆

| 架构 | 类比 |
|------|------|
| **融合式** | 搬到"全能社区"，衣食住行一站式 |
| **分离式** | 住在原来的小区，另外找个超市买菜 |

**已经有小区了**（MySQL 技术栈），**没必要为了买菜搬家**——除非：

- 超市搬进小区（迁业务库到 PostgreSQL）
- **或者**小区旁边就有超市（**Redis Stack**）

---

## 八、下一步

```
1. 学习阶段：agent-rag 用 pgvector 跑通 RAG 全流程
2. 生产评估：
   - 数据量多大？
   - 团队有 PostgreSQL 运维经验吗？
   - 已有 Redis 吗？
   - 需要独立向量库专业功能吗？
3. 按评估结果选择：Redis Stack / Qdrant / pgvector / Milvus
```

**核心心态**：不要因为学习用了 pgvector，生产就必须用 pgvector——**技术选型服务于场景，不是服务于学习路径**。